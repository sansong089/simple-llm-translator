# Project Rules

- 代码与文档必须联动维护。
- 任何代码修改完成后，必须同步检查并更新受影响的文档，包括但不限于 `docs/specification.md` 和 `docs/technical-design.md`。
- 如果本次代码变更没有对应文档差异，最终说明中必须明确说明原因。
- 真实验证优先使用真实浏览器测试。
- 测试时优先连接 `127.0.0.1:9222` 的现有浏览器；仅当该端口没有可用浏览器实例时，才允许启动新的浏览器实例。
- 真实验证优先使用 GPT API，不要优先使用 mock；mock 仅用于隔离诊断、最小复现或用户明确允许的场景。
- 任何代码修改完成后，必须至少执行一轮与本次变更相关的回归测试。
